import 'package:flutter/cupertino.dart';

class CreateEvent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Create Event Page', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),
    );
  }
}